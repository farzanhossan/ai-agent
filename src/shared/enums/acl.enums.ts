export enum ENUM_ACL_DEFAULT_ROLES {
  SUPER_ADMIN = 'Super Admin',
  PARTICIPANT = 'Participant',
  INSTRUCTOR = 'Instructor',
  MANAGER = 'Manager',
}

export enum ENUM_IDENTIFIER_TYPE {
  SUPER_ADMIN = 'Super Admin',
  PARTICIPANT = 'Participant',
  INSTRUCTOR = 'Instructor',
  MANAGER = 'Manager',
}

export enum ENUM_AUTH_PROVIDERS {
  SYSTEM = 'system',
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}
